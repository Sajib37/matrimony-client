import { Helmet } from "react-helmet-async";

const ContactRequest = () => {
    return (
        <section>
            <Helmet>
                <title>Matrimony || My contact Requests</title>
            </Helmet>
        </section>
    );
};

export default ContactRequest;